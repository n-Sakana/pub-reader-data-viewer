# 台帳の保護・削除・復元

従来版と固定版で同じ台帳保護を使えます。一連の操作は [サンプルの手順](samples/README.md)、①②③の結合と決済条件は [業務設定の説明](PAYMENT-GUIDE.md) を参照してください。

## dataからの更新

設定の `paths.dataDir` にある①〜③を差し替え、「データ更新」を実行します。一部だけ差し替えた場合も、残っている入力と合わせて読み直します。ファイルを置くだけでは書き込みません。設定された入力が欠ける場合や、形式・見出しが違う場合は案内して停止し、共有台帳を変更しません。

| 共有台帳の現在の行 | 今回の業務設定での更新結果 |
|---|---|
| 処理済を送信・保存済み | 全項目と処理状態を保持 |
| 未処理で今回の結合結果にもある | 今回の内容に更新。空欄への訂正も反映 |
| 今回の結合結果にない既存行 | 内容と状態を保持 |
| 新しいオーダーID | 未処理で追加 |
| 削除保管中のオーダーID | 再登場しても復活せず、保管内容も保持 |

保護の基準は、共有ロック下で読み直した台帳です。手元の未送信変更は使いません。未送信変更と入力訂正が競合した案件は、送信できない変更として残します。処理済の内容を訂正したい場合は「未処理」に戻して送信し、その後にデータ更新を実行します。処理済だった履歴を永久ロックにはしません。入力ファイルの更新日時を各行の新旧判定には使いません。

## 削除と復元

「データ削除」は④の**同じ行のカード番号と申請番号の組**に一致し、共有台帳へ処理済を保存した行だけが対象です。画面で処理済にしただけなら先に「変更をまとめて送信」を実行します。

削除した全項目・処理状態・削除日時は同じXLSXへ保管します。通常行と保管行を一体で保存するため、保存失敗で通常行だけ消えることはありません。保管行は通常検索・帳票出力・通常件数に含めません。

1. 主画面の「削除済み」を開きます。
2. 識別キーのボタンで全内容を確認し、戻す行にチェックします。詳細欄は縦スクロールできます。100件ごとのページをまたいで選択できます。
3. 「選択したレコードを復元」を押して確認します。削除時の全内容と処理状態で通常台帳へ戻り、保管一覧から外れます。

復元時も最新の保管内容を共有ロック下で確認します。別の端末が既に復元した行や、同じ識別キーの通常行がある場合は上書きしません。一覧を開き直して確認します。復元した処理済行は更新から保護されます。ただし④に残っていれば、次回の「データ削除」の対象にはなります。

## 固定するJSONの範囲

台帳には作成時の業務定義と入力見出しを記録します。JSONの直接編集も検出し、定義が違えば更新・送信・削除・復元を拒否します。入力差分がない更新でも確認します。元の設定へ戻すか、別の台帳で最初から検証してください。

| 変更 | 同じ台帳での扱い |
|---|---|
| JSONのキー順・空白・コメント | 変更可能 |
| データ内容、同じ形式の入力ファイル名・置き場所、台帳・ログの場所 | 変更可能 |
| 表示文言・寸法、検索・監視設定 | 変更可能 |
| CSV/XLSX形式・文字コード・区切り・シート・見出し行・列構造・キー・型 | 変更不可 |
| 抽出・計算・照合・結合・追加・更新・保持・削除の定義 | 変更不可 |
| 台帳識別・保存列・保存する状態値・初期状態・保護対象状態 | 変更不可 |

保護対象は `data.ledger.protectStates` に**保存値**で指定します。今回の設定は `["TRUE"]` で、`screen.workState.states` の処理済の `stored` と一致します。表示名ではありません。省略した汎用設定は保護状態を指定しません。作成後にこの値を抜く・変える操作も拒否します。

従来版の復元ボタンは `screen.sections` の操作ボタン欄に `{"action":"restoreRecords","text":"削除済み"}` を置きます。固定版はHTMLの既定ボタンを使います。許可された設定の変更も、設定画面の案内に従うか再起動して反映します。

## 旧台帳の移行

業務定義の記録がない旧台帳を、任意の現在のJSONへ自動で結び付けません。読み取れる台帳も、書き込みには移行が必要です。**作成時JSONが不明なら、元台帳と設定を保管して管理担当へ確認してください。** アプリだけでは過去の結合定義を証明できません。

1. 全端末の未送信変更を旧アプリで送信し、全員が旧台帳の操作を終えます。台帳と作成時JSONを控えます。
2. 作成時JSONを `original-settings.json` として保存します。移行先JSONは同じ業務定義にし、保護対象状態と復元ボタンを追加します。入力ファイルの場所は変更できます。
3. 新しいアプリのフォルダで次を実行します。パスは手元のものに直し、出力先は存在しない新しいファイル名にします。

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Migrate-Ledger.ps1 -OriginalConfig C:\trial\original-settings.json -Config C:\trial\settings.json -DataDir C:\trial\data -Ledger C:\trial\old-ledger.xlsx -Output C:\trial\migrated-ledger.xlsx -ConfirmOriginalDefinition
```

`-ConfirmOriginalDefinition` は「指定したJSONがこの旧台帳を作った設定」と利用者が確認した印です。推測したJSONに付けないでください。定義不一致、使用中のロック、行や状態の読取警告、既存の出力先があれば停止します。元台帳を書き換えず、全内容と保存済み状態を別ファイルへ移します。

4. 出力の行と状態を確認して、全端末の `paths.ledger` を移行先へ変更します。アプリとJSONを今回の版に揃えて使用します。未送信変更は別の台帳名へ自動移行しません。

APP基準だった旧世代など、識別キー・保存列が異なる台帳は同一定義移行の対象外です。旧アプリで既に削除した行は、元台帳に全内容がないため復元できません。

統合XLSXはアプリが管理します。Excelでの上書き保存・列変更は避け、内容の確認や加工には帳票出力のCSVを使います。旧アプリは新しい保存形式を拒否します。新旧アプリで同じ台帳を混用しません。

## 自動読取と前面化

今回の業務設定では、**自動読取で案件が確定し、決済判定が済の場合だけ、その案件を自動で処理済みにします。** 未決済でも読取と照会は行います。0件・候補未選択・候補キャンセル・未決済・判定不能・判定エラーでは自動付与しません。手入力の照会では自動付与せず、手動の処理済み／未処理変更は維持します。自動付与も手元の未送信変更なので、共有台帳へ保存するには「変更をまとめて送信」が必要です。

`screen.workState` の既存項目へ次を加えます。

```json
{
  "trigger": "automatic",
  "automaticWhen": { "judgment": "paymentStatus", "result": "paid" }
}
```

`judgment` は `screen.judgments` のID、`result` はその `results` の結果IDです。表示名「済」や特定の列名を自動付与処理へ埋め込まず、画面と同じ判定を使います。不明なIDや `undefined` / `error` の指定は設定エラーです。汎用設定で `automaticWhen` を省略した場合は、従来の条件なし自動付与を維持します。`trigger: "manual"` では自動付与しません。この画面上の操作方針は、変更後に再起動して反映します。

新しい有効番号を監視元から受け付けたとき、一度だけ前面化を試みます。最小化中は元の通常状態または最大化状態へ戻します。複数件なら候補一覧、0件なら主画面に「見つかりません」を表示します。

同じ値の連続ポーリング、無効値、手入力の単一検索では、この自動前面化を行いません。監視元が空欄になってから同じ番号が入った場合は新しい読取です。常時最前面にはしません。

Windowsが拒否した場合も検索・監視を続け、ログの `detect` 行に `foreground=refused` を残します。実際に前面を確認できた場合は `foreground=accepted` です。[Windowsの前面化条件](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setforegroundwindow)に従うため、すべての環境で成功する保証はありません。

## 確認範囲

架空入力のXLSX更新・保護・削除・復元と、WPF/WebView2の実窓を検査しています。自動前面化は、OSが許可しない条件と、検査用読取元が `AllowSetForegroundWindow` で許可する条件を区別して確認しています。後者の成功を実カードリーダーでの成功とはしていません。

会社配布PCの速度、実カードリーダー、本番データ・本番共有先、実際の複数PC同時操作、異なるDPIは未確認です。開発機の100件サンプルの成功を会社PCの性能合格にはしていません。
