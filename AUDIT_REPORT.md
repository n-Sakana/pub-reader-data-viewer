# Reader Data Viewer バグ点検・修正報告書

- 点検日：2026年9月8日（日本時間）
- 対象：提供された`ReaderDataViewer.zip`内のローカルファイル一式
- 原ZIP SHA-256：`fcabd6022009117a9211cb6373ce8751307d606cffa1b1cf3f7a8dab59832d5a`
- 成果物：`ReaderDataViewer_fixed.zip`、本報告書。ZIP内にも`AUDIT_REPORT.md`として同内容を収録

## 1. 結論

**修正すべき不具合・データ保護上の弱点を複数確認し、ソースと設定を修正しました。特に、同じ行の作業状態を後から送信した人の値で上書きできる問題、古い検索結果が操作対象へ戻る問題、既存ファイルを出力等で壊し得る経路を重点的に扱いました。**

ただし、これは「全ての環境で動作を保証する完成判定」ではありません。**この作業環境はLinuxであり、Windows上のC#コンパイル、WPF／WebView2起動、UI Automation、実際の複数PC・SMB共有の試験は実施できていません。** 当該環境を使う検査コードと受入手順を同梱しました。本番台帳へ置き換える前に、検証用コピーで実施してください。

| ご依頼の観点 | 判定 |
|---|---|
| 一般的な動作不能・誤動作 | 画面側の既存不具合3件をブラウザーで再現し修正版で解消確認。その他のデータ処理・状態遷移の問題はコードを修正。ただしWindows本体のビルド合否・実動作は未確認 |
| JSON変更による用途の柔軟性 | 表・結合／加工ジョブ・検索・判定・表示・状態定義は変更可能。CSV形式・出力列等の過度な制約を緩和。ただし任意の台帳アプリ、Excel共同編集、汎用ワークフローではない |
| 複数人・複数PCの同時編集 | 元から共有書込ロックは存在。しかし行の論理競合チェックが不足。変更前照合・再送整理等を追加したが、実共有環境での合格は未確認 |
| 誤操作・紛らわしい挙動 | IME、クリア、未送信と共有保存の違い、CSV出力、既定の自動状態変更、削除条件の意味等を修正または明示 |

## 2. 検証範囲と証拠の区別

元ZIPは41ファイルです。元のアプリC#は21ファイルで、今回ファイル保護とCSV解析の2ファイルを追加し23ファイルになりました。設定、起動スクリプト、JavaScript、CSV／XLSXの読み書き、未送信ストア、共有ロック、処理ジョブ、UIの状態遷移を調査しました。

実測と推論を区別します。以下のF01〜F28は修正・防御追加・配布整備の整理単位であり、独立した実測バグを28件再現したという意味ではありません。コードから問題経路を確認したものと、実際にブラウザーで再現したものを分けて記載します。

### 実施した検査

| 検査 | 実結果 | 証跡・範囲 |
|---|---|---|
| 修正版ブラウザーJS／DOM | **14/14通過** | `tests/results/web-results.json`。Chromium 144.0.7559.96、架空データと模擬ブリッジ |
| 元版比較 | **選定3件の問題を再現** | `tests/results/original-web-reproductions.json`。旧版に対するFAILは問題再現を意味する |
| 静的・サンプル整合 | **30/30通過** | `tests/results/static-results.json`。C#字句・括弧24件、参照名、JS構文、設定／入力整合、元ファイル維持等 |
| JavaScript構文 | 通過 | Node.js v22.16.0の`node --check`。上記静的検査にも含む |
| 入力・DLL・ライセンス | 元とバイト一致 | `data/`、`lib/`、ライセンス等12ファイル。元の全41ファイルが残存 |

ブラウザーでは配布HTML／CSS／JSを実行し、実ファイル読み込みの代わりに注入しています。WebView2の発信元検証、C#の受信処理、Windowsのファイル操作は模擬していません。IME試験も合成イベントであり、実際の日本語IME製品の入力経路全体の検証ではありません。

### 提供したが実施していない検査

`build.bat compile`はWindowsでの全C#コンパイル検査です。`build.bat test`は**C#回帰試験50項目**で、JSON、競合、再送、CSV、XLSX、ファイル保護、設定保存、ロック、8スレッドの書込競合等を含みます。`tests/fixtures/`の設定・CSVと新規一時フォルダーを使用し、本番台帳を更新しません。

**これら50件は未実行です。8スレッド試験も合格したとは報告しません。** 実施状態は`tests/results/native-status.json`に`NOT_EXECUTED`として記録しました。C#の静的字句検査をネイティブコンパイル成功と見なすことはできません。実際の2台PC／SMB、電源断、ネットワーク断、大規模データ、性能上限、DPI、対象アプリのUI Automation互換性も未確認です。

テスト実時刻は結果ファイル内のUTCを参照してください。日本時間2026-09-08の検査がUTCでは2026-09-07になっているものがあります。

## 3. 主要な修正・防御追加

### F01｜高｜同一行の状態変更が後勝ちになる

**元の問題・想定経路：** 元版は識別キーと内容ハッシュだけを照合し、現在の共有状態を問わず未送信値で上書きしていた。

**対応：** 変更前の状態をローカルに保存し、ロック内の最新状態と照合。不一致はstate-conflictとして保持。

**場所／検証：** `src/Rdv3Shared.cs:166`。コード修正・C#試験提供。

### F02｜高｜送信後の障害・再送で二重反映／再計上

**元の問題・想定経路：** XLSX保存後、通知やローカル未送信整理に失敗して再送すると、既に同じ値でも再書込・計上していた。

**対応：** 最新値が希望値なら反映済みとして整理し、変更件数を増やさず不要なXLSX書込を省略。

**場所／検証：** `src/Rdv3App.cs:1143`。コード修正・C#試験提供。

### F03｜高｜未送信変更を再編集すると古い競合の由来を失う

**元の問題・想定経路：** 既存の未送信変更を作り直す際に元の内容・変更前の情報を保てないと、古い変更を新しい行へ持ち越す危険がある。

**対応：** 最初の変更前状態と内容ハッシュを保持。内容変更・行消失・状態競合を区別し、旧形式も保守的に扱う。

**場所／検証：** `src/Rdv3Shared.cs:104`。コード修正・C#試験提供。

### F04｜高｜同じローカル未送信ファイルの同時使用

**元の問題・想定経路：** プロセスごとの不安定な文字列ハッシュやセッション境界に依存した多重起動判定だけでは、同じローカルストアの排他が弱い。

**対応：** 安定したSHA-256の識別子と、pendingファイルに対応するFileShare.Noneのセッションハンドルを併用。

**場所／検証：** `src/Rdv3Files.cs:153`。コード修正・C#試験提供。

### F05｜高｜読めない既存台帳を再構築で上書き

**元の問題・想定経路：** 読込エラーを台帳再作成の許可へつなげる経路があり、既存の作業状態や内容を失うおそれ。

**対応：** 既存ファイルの読込失敗は再作成せず停止／直前の有効表示を維持。権限エラーを単なる不存在として扱わない確認を追加。

**場所／検証：** `src/Rdv3App.cs:389`。コード修正・Windows未検証。

### F06｜高｜更新の承認待ち中に他者が内容を変更

**元の問題・想定経路：** 確認した版と書込み時の版が違っても古い承認のまま内容更新できる。

**対応：** ロック内の再読込と承認時の内容を照合し、内容が変わっていれば再確認を要求。状態だけの変化は最新値を基に処理。

**場所／検証：** `src/Rdv3App.cs:545`。コード修正・Windows未検証。

### F07｜中｜「入力差分なし」で共有状態が古いままになる

**元の問題・想定経路：** 内容だけの比較や、既存メモリーをそのまま継続する分岐では、他者が更新した作業状態を取り込まない。

**対応：** 状態も含む比較を使用し、差分なしでも今回読み込んだ台帳を採用。台帳の未知／空の作業状態も拒否。

**場所／検証：** `src/Rdv3App.cs:468`。コード修正・Windows未検証。

### F08｜高｜クリア・再検索・台帳切替後に古い結果が復活

**元の問題・想定経路：** 検索中に表示をクリア／入力変更しても検索IDや参照台帳を十分に失効させていなかった。

**対応：** 入力変更・クリア・新検索・再読込で結果を無効化し、検索IDと元台帳配列の一致を確認してから表示。候補選択の前後でも照合。

**場所／検証：** `src/Rdv3App.cs:832`。コード修正・Windows非同期経路未検証。

### F09｜高｜確認画面や処理中の監視が古い対象へ作用

**元の問題・想定経路：** モーダル表示中や保存中に、別の検出・台帳切替が干渉し得る。

**対応：** モーダル多重化、保存中・モーダル中の検出受付／台帳切替を抑制。受け付けた検出だけを検出済みとして記録。

**場所／検証：** `src/Rdv3Watch.cs:66`。コード修正・UIA実機未検証。

### F10｜中｜IMEのEnterが検索・承認になる

**元の問題・想定経路：** 変換確定のEnterを通常のEnterとして処理し、検索等を送出していた。

**対応：** isComposing／keyCode 229を捕捉して通常操作を止める。入力正規化は変換中に行わず、変換終了時に行う。

**場所／検証：** `web/app.js:1472`。元版再現・修正版ブラウザー通過。

### F11｜中｜貼付け後の入力表示と送信値が一致しない

**元の問題・想定経路：** 改行やリッチなDOMが入力欄に残り、実際の検索キーと見え方がずれる。

**対応：** DOMと値を同じプレーンテキストへ正規化し、改行・タブ除去と最大長を適用。

**場所／検証：** `web/app.js:202`。元版再現・修正版ブラウザー通過。

### F12｜中｜同種ジョブボタンでDOM IDが重複

**元の問題・想定経路：** 複数のupdateRecords等を設定すると同じIDになり、参照・操作が曖昧になる。

**対応：** 各アクションに連番IDを付け、ジョブ識別子を保持。再描画時は連番をリセット。

**場所／検証：** `web/app.js:113`。元版再現・修正版ブラウザー通過。

### F13｜高｜出力やログ設定で入力等を壊せる

**元の問題・想定経路：** 出力先が入力CSVや設定等と衝突し得る。削除用のファイル入力も保護対象に含める必要がある。

**対応：** 通常テーブルとジョブ固有入力を含めてパス衝突を確認。出力は.csv限定、新規のみ。同一ディレクトリーの一時ファイルから非上書きMoveを使用。

**場所／検証：** `src/Rdv3Files.cs:78`。コード修正・C#試験提供。拡張子UIはブラウザー通過。

### F14｜中｜CSVの数式解釈・同名列・出力中の変更

**元の問題・想定経路：** CSVを表計算ソフトで開いた際の数式扱い、異なる表の同じ列名、出力中の変更で紛らわしい結果が生じる。

**対応：** 数式無効化を選択可能にし既定ON。重複見出しを識別可能にし、配列のスナップショットで出力。出力結果が確定するまで書込／終了ガードを維持。

**場所／検証：** `src/Rdv3App.cs:1461`。コード修正・C#試験提供。選択UIはブラウザー通過。

### F15｜中｜表示ラベル未登録の保存列を出力できない

**元の問題・想定経路：** data.labelsへ載せないとCSV出力の候補・既定欄の扱いが過度に制約された。

**対応：** 保存列を出力候補に追加し、ラベルがなくても参照名で選べるようにした。

**場所／検証：** `src/Rdv3Modals.cs:441`。コード修正・C#試験提供。

### F16｜中｜設定画面同士／外部編集との上書き競合

**元の問題・想定経路：** 再読込だけでは、古い画面がpaths/search/watchの新しい値を消し得る。

**対応：** 画面を開く時点の設定を読み、保存時に内容ハッシュと専用編集ロックを確認。失敗時は新しい設定を保持。

**場所／検証：** `src/Rdv3Config.cs:415`。コード修正・C#試験提供。

### F17｜中｜検索正規表現の部分一致と実行時間無制限

**元の問題・想定経路：** 元版IsMatchはキー全体一致ではなく、設定によっては余分な文字を含むキーを通す。複雑な式が長時間を要し得る。

**対応：** キー全体一致のラッパー、長さ制限、250msの正規表現タイムアウトを導入。判定／抽出用の式にも期限を設定。

**場所／検証：** `src/Rdv3Config.cs:390`。コード修正・C#試験提供。

### F18｜中｜独自JSONパーサーの不正入力の受理

**元の問題・想定経路：** 不正な数値表記、生の制御文字、未終了コメント等で設定の意味が曖昧になる余地。

**対応：** JSONCのコメント／末尾カンマは維持しつつ、数値文法、文字、サロゲート、深さ等の検証を強化。

**場所／検証：** `src/Rdv3Json.cs`。コード修正・C#試験提供。

### F19｜高｜一般的なCSV・文字コードで読み込み不能／値変化

**元の問題・想定経路：** 引用符付きCSV、複数行フィールド、UTF-16等を単純バイト分割すると不正に分割される。空表や不正エンコーディングも問題。

**対応：** 文字デコードによるCSV解析を追加し、BOM整合、不正引用符・列数・文字コードを検証。ヘッダーのみ0件を許可、物理行番号を保持。

**場所／検証：** `src/Rdv3Csv.cs` / `src/Rdv3Core.cs`。コード修正・C#試験提供。

### F20｜中｜ASCII索引へ非ASCIIバイトを渡すと別キー扱い

**元の問題・想定経路：** 代替文字への変換によって、元の文字でないキーへ一致する可能性。

**対応：** 固定ASCII検索経路に非ASCIIバイトを渡した場合は一致させない。

**場所／検証：** `src/Rdv3Index.cs`。コード修正・C#試験提供。

### F21｜高｜XLSXの論理シート順・文字列の扱い

**元の問題・想定経路：** sheet1.xml固定、ふりがな文字列の混入、CR/LFや空白の変化等が読み違いにつながる。

**対応：** workbook/relsに従うシート選択、rPhの除外、文字列空白・CR保存、ヘッダーのみの入力表を扱う修正。

**場所／検証：** `src/Rdv3Xlsx.cs:538`。コード修正・C#試験提供。

### F22｜高｜XLSXの不正形状・余分なデータを次回保存で失う

**元の問題・想定経路：** 非空の余分な列、追加シート、列数・状態数不一致、エラーセルや未計算数式等を曖昧に扱うと、上書き時の欠落につながる。

**対応：** 追加シート・非空の余分な列を拒否。セル参照、共有文字列番号、値形状、行列上限、XML文字、セル長、数式キャッシュを検証。

**場所／検証：** `src/Rdv3Xlsx.cs:591`。コード修正・C#試験提供。

### F23｜高｜異なる保存定義のクライアント混在

**元の問題・想定経路：** 見出しだけが同じでも識別キーや状態の保存意味が違い得る。

**対応：** 新たに書く台帳へ保存契約ハッシュを付与し、修正版で不一致を拒否。旧台帳は従来ヘッダー検証を残すため移行前確認が必要。

**場所／検証：** `src/Rdv3Files.cs:162`。防御追加・C#試験提供。

### F24｜中｜通知更新失敗・古い版の復元を見逃す

**元の問題・想定経路：** 台帳保存と版通知は別処理。版番号だけの比較やUI上の定期I/Oは反映遅れ・画面待ちにつながる。

**対応：** 書込者・時刻も含む版比較、XLSX時刻／サイズの補助検知、定期I/Oのワーカー化、読込失敗時の再試行間隔を追加。

**場所／検証：** `src/Rdv3App.cs:1565`。部分改善・実共有試験未実施。

### F25｜中｜競合変更を安全に整理する操作がない

**元の問題・想定経路：** 照合不能の未送信分が残り続け、どう処理すべきか分かりにくい。

**対応：** 理由別の一覧、保持を既定とする操作、追加確認とバックアップ付きのローカル破棄を追加。

**場所／検証：** `src/Rdv3App.cs:1214`。コード修正。保持／破棄UIはブラウザー通過。

### F26｜中｜自動検出の既定動作が状態を変える

**元の問題・想定経路：** 元設定はtrigger省略、元コード既定automaticのため、検索のつもりの監視が作業状態を変更し得る。

**対応：** 既定をmanualへ変更し、同梱JSONにも明記。従来のautomaticは明示指定で選べる。

**場所／検証：** `settings.json` / `src/Rdv3Screen.cs`。意図的な既定変更・静的確認・C#試験提供。

### F27｜低〜中｜Windowsホスト・画面メッセージの境界

**元の問題・想定経路：** 32ビット起動元では64ビット必須アプリが起動できない場合があり、ダイアログ側のナビゲーション制限も主画面と揃っていなかった。

**対応：** 存在すればSysnativeのPowerShellを選択。主画面／ダイアログで外部遷移・新窓とメッセージ発信元を制限。イベント解除も整備。

**場所／検証：** `ReaderDataViewer.cmd` / `ReaderDataViewer.vbs` / `src/02_MainWindow.cs`。防御追加・Windows未検証。外部攻撃の実証ではない。

### F28｜低｜READMEの手順と配布物が不一致

**元の問題・想定経路：** build.bat、設定資料、検査・生成スクリプト、過去版等がある説明だが、元ZIPには含まれていなかった。

**対応：** この配布物で利用できるcompile/test入口、設定／共有運用資料、検査コードを追加。元READMEは履歴資料として保全。

**場所／検証：** `README.md` / `build.bat` / `docs/` / `tests/`。配布物確認・修正。

## 4. 同時編集についての評価

### 元のロックを誤って否定しないこと

元実装にも、`.lock`を`FileMode.CreateNew`で開き、ハンドルを保持してXLSXを一時ファイルから置換する仕組みがありました。`FileShare.Read`と`DeleteOnClose`の組合せであり、Windowsの共有削除許可は付けていません。そのため「古い時刻のロックファイルなら、生きている書込ロックでも必ず削除される」とは判断していません。Windowsの共有モードと削除条件はMicrosoftの仕様を参照しました。[S1][S2][S3]

今回の中心的な問題は**ファイルを同時に壊す排他の不足だけでなく、先行する他者の状態変更を論理的に消すこと**です。例えば3状態の設定で、同じ変更前状態からAが「完了」、Bが「保留」を未送信に持った場合、元版はA送信後にBが送信するとAの状態を上書きできました。修正版はBの変更前状態と現在の共有状態が違うため、競合として保持する設計です。

### 修正版でも保証しないこと

XLSX、`.version`、ローカル未送信データは別ファイルで、まとめて原子的に更新されるわけではありません。台帳保存後に通知だけ失敗する、保存済みだが未送信に残る等の中間状態はあり得ます。反映済み判定と時刻／サイズによる補助検知を入れましたが、完全なトランザクションや全障害復旧ではありません。

同じ値へ戻った履歴（ABA）、同じ識別キー・同内容の行の削除と再作成、同時に設定だけ変えた旧クライアント、共有ロックを無視する別プログラム、時刻・サイズも一致させた復元等には限界があります。行ごとの単調増加版番号、永続的な履歴、認証・承認権限、サーバー側の取引処理は未実装です。

SMB等のファイル共有では、実際にどのプロトコル・ファイルシステム・権限で開くかによってロックとの相互作用が変わります。一般的なWindows APIの仕様だけで個別NASの正しさを証明していません。[S1][S4] 全PCを同じ修正版、同じ共有パス、互換する保存定義へ揃えることが前提です。同期フォルダーの各PCローカルコピーの編集は、この共有排他と同じではありません。

## 5. JSONによる汎用化の到達点と制約

定義済みの表、識別キー、検索対象、型、ラベル、データ加工ジョブ、判定規則、候補一覧、画面部品、状態を変更する範囲では、用途変更の土台があります。引用符付きCSV、文字コード、ラベル未登録列の出力、複数ジョブボタン等を今回改善しました。

一方、JSONに任意の項目を追加すれば何でも実現できる構造ではありません。以下は仕様上の境界です。

| 項目 | 残る制約・要設計点 |
|---|---|
| 編集できる値 | 対話的な編集は主としてアプリ管理の作業状態1列。入力元の各セルを自由に編集する画面ではない |
| 状態遷移 | 複数状態は可能だが、同一状態から複数の業務操作を選ぶ一般的な分岐フローや多段承認はない |
| 入力の必須性 | `data.tables`に列挙した表は起動時に確認する。あるジョブで使わない表でも欠落が起動に影響し得る |
| 識別キー | 全行で一意かつ安定したキーが必要。重複除去は最初の行の採用であって任意集約ではない |
| XLSX | 入力の先頭シートが対象。任意シート指定、式再計算、Excelの表示書式や日付シリアルの汎用変換はない |
| 共有台帳 | アプリ専用の1シート。Excelの自由な書式・追加シート等の保持はしない。追加シートは拒否する |
| 型・数値 | 不変カルチャーと指定日付書式を前提。数値らしい文字列の意味、桁区切り、先頭ゼロ等は業務側で定義が必要 |
| 設定の切替 | JSONの外部編集だけでは全定義を即時再読込しない。データ・画面・保存先の変更等は再起動・移行が必要 |
| 監視設定画面 | 複数対象や有効無効はJSONで確認。選び直しただけでは元の無効状態を自動解除しない |
| 設定契約 | 台帳内ハッシュは保存構造の一部を照合するもの。設定全体の同一性や業務意味の一致の証明ではない |

詳細と有効な設定部分例は`docs/settings.md`に記載しました。特にUnicode・可変長キーは`characters: "unicode"`と`length: "variable"`で指定します。内部C#のプロパティ名`Ascii`や`FixedLength`をそのままJSONの項目名にしないでください。

## 6. 使い方で起き得る意図しない挙動

### ローカル保存と共有反映は別

状態ボタンでの「保存」はローカル未送信ファイルへの保存です。共有反映には送信操作が必要です。PCを替えると前PCの未送信は見えません。CSV出力には当該PCのローカル未送信状態が入り、他者の未送信や未読込の変更は入りません。CSVを全社の確定状態と見なすには送信・最新読込の手順が必要です。

### 同梱の削除条件は2列ペアではない

`delete.csv`の`key2`と`b_ref`は別々の入力集合として扱われ、その両方に該当する**レコード集合の積集合**を削除します。CSVの同じ行の2列を組にして照合しているわけではありません。例えばリストに`(K1,R2)`と`(K2,R1)`がある場合、集合条件だけなら`(K1,R1)`というレコードも該当し得ます。

同梱データをPythonで独立に照合した範囲では、集合条件・ペア条件とも342行が該当しました。ただしC#の削除処理を実行した結果ではありません。別データでは意味が分かれます。業務意図が不明なまま削除ロジックを変えることはせず、仕様注意として残しました。ペア一致が必要なら識別可能な複合キーを入力側で用意する等、設計の変更が必要です。

### 空キー、状態リセット、初回ジョブ

同梱A/B/Cは各1,000行です。Bには空キーが10行あり、`empty: "skip"`により有効キーは990行です。入力変更時の`onSourceChange: "reset"`は、修正版でも意図的に作業状態を初期へ戻す設定です。最初のupdateジョブが起動時の対象なので、ジョブを並べ替えるだけでも実行内容が変わります。

### 自動監視は全スキャン記録装置ではない

同じ値が表示されたままなら毎回新規検出するわけではなく、処理中に現れて消えた一時的な値を全件記録する保証もありません。アプリのクリアは外部アプリの値を消しません。自動変更を選ぶ場合は、対象アプリ、検出頻度、確認の有無を業務として検証してください。

### ExcelでのCSV解釈、直接編集

数式無効化のONは`=`, `+`, `-`, `@`等で始まる値にアポストロフィを付けるため、負数も含め元のCSV値が変わる場合があります。機械連携にはOFFの選択を用意しました。先頭ゼロや日付のExcel自動変換は別問題であり、CSVの取り込み時に文字列列を指定する等の操作が必要です。稼働中の共有台帳をExcelで直接編集する用途は対象外です。

### 共有障害の待ち時間

設定のtimeoutはOS I/Oの強制停止期限ではありません。定期通知確認はワーカーへ移しましたが、同期I/O、ログ、ファイル情報取得等の待ち時間が残る可能性があります。保存結果が決まらない限り通常終了を保留する設計を維持しています。共有断時の操作性は実機試験が必要です。

## 7. 互換性に影響する変更

| 変更 | 移行時の対応 |
|---|---|
| trigger既定automatic→manual | 自動状態変更を本当に必要とする場合だけ、検証後に`automatic`を明記 |
| CSVは既存ファイルを上書きしない | 毎回新しい名前を指定。不要な旧ファイルの削除は確認して別途行う |
| 数式無効化は既定ON | 元の値が必要な機械連携はOFF。Excel向けの安全化との使い分けを明示 |
| 同名CSV列の見出し | 衝突する場合は`A.id`等へ変わる。後段システムの列名参照を確認 |
| 検索パターンの全体一致 | 旧版の部分一致に依存した設定を見直す |
| 不正CSV／JSON／XLSXの拒否強化 | 従来たまたま読めた不正入力は、原因を直して使用する |
| 追加シートのある台帳を拒否 | 全PC停止・保全後、台帳以外のデータを別ファイルへ分離 |
| 未送信形式V2と保存契約 | 全PC停止・各PCの未送信を含むバックアップ・全PC同時更新が必要 |

`RDV-PENDING-1`は読めますが変更前の状態が分からないため、未反映分を無条件送信しません。`RDV-PENDING-2`を旧版へ戻して読ませることはできません。旧台帳には契約情報がないため、最初の書込みで付与する前に列と状態の意味を確認してください。詳細な順序は`docs/shared-ledger.md`にあります。

## 8. 本番投入前に必要な確認

まずバックアップを取り、本番と切り離したWindows検証用コピーで`build.bat compile`と`build.bat test`を実施します。次に実際に使うファイルサーバーと2台PCで、別行の同時送信、同じ行の異なる状態、送信後の中断／再送、内容更新・削除との競合、Excel占有、権限拒否、共有断、旧形式、異なる保存定義、再起動・復元を確認します。

期待値付きの14分類の受入表を`docs/shared-ledger.md`に同梱しました。試験の合否、OS・WebView2 Runtimeの版、共有プロトコル、権限、最大件数を記録してください。**この表は試験計画であり、今回の実施済み結果ではありません。**

## 9. 配布物の保全と追加ファイル

元データ`tableA.csv`、`tableB.csv`、`tableC.csv`、`delete.csv`、元ログ、WebView2のDLLと表示文書、ライセンス類は変更していません。元READMEは現状に合う内容へ書き直し、原文を`docs/README.original.md`に保全しました。新しい実行バイナリーをコンパイルして同梱したのではなく、起動時コンパイル用の修正版ソースを返しています。

追加物はファイル保護／CSV解析ソース、compile/test起動入口、C#50ケース、ブラウザー／静的検査、隔離サンプル、検査結果、設定／共有運用資料、原README、変更マニフェストと差分です。`docs/change-manifest.json`に元ファイルと修正版のSHA-256、`docs/source-changes.patch`に主なソース・起動・設定・画面の差分を収録します。パッチはレビュー用であり、配布ZIPのソースへ再適用する必要はありません。

## 10. 根拠資料

ローカル実装の根拠はF01〜F28のパス、回帰検査コード、結果JSON、差分を参照してください。ファイル位置の行番号はこの修正版のものです。外部資料はプラットフォームの意味を確認するために用い、個別環境の実証結果とは分けています。

- [S1] Microsoft, CreateFileA — dwShareMode、共有削除等。https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-createfilea
- [S2] Microsoft, FileOptions — DeleteOnClose等。https://learn.microsoft.com/en-us/dotnet/api/system.io.fileoptions
- [S3] Microsoft, DeleteFile — オープン中のハンドルと削除条件。https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-deletefile
- [S4] Microsoft, Managing file locks — ファイル共有でのロックとの相互作用の例。個別NASへの適合保証ではない。https://learn.microsoft.com/en-us/rest/api/storageservices/managing-file-locks
- [S5] Microsoft, WebView2 user data folders — ユーザーデータ保存と実行環境上の考慮点。https://learn.microsoft.com/en-us/microsoft-edge/webview2/concepts/user-data-folder

既存のWebView2 DLLは更新していません。依存DLLの脆弱性データベース照合や全ての利用端末でのRuntime互換性確認は、今回の結果に含みません。
