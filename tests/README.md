# 検証コードと実行結果

## 2026-09-11 固定版レビュー修正

今回の固定版の対象・再現・実窓検証・仕様比較は [REVIEW-20260911.md](REVIEW-20260911.md)。再実行入口は `Test-Review.ps1`、レビュー梱包は `tools/Build-Review.ps1 -Build <build.batの出力ディレクトリ>`。以下は過去の版の記録であり、今回の完了条件を追加するものではない。

## 2026-09-10 台帳保護・復元

追加した検査の実行方法・期待値・実窓の確認条件は [PROTECTION.md](PROTECTION.md) を参照してください。両版の中核120件、旧台帳移行、復元UI、4種類のJSON書込みガード、期間混在サンプルと配布物を確認しました。前面化はOSの通常条件と検査元の明示許可条件を区別し、会社実機・実カードリーダー・本番共有・他DPIは未確認です。

## 2026-09-09 想定外レコードの除外・日本語診断・送信後の表示

先生が承認した「想定外のレコードは除外して続け、構造の違いと保存済み台帳の破損は停止する」に合わせ、入力・式・集計・数値条件・並べ替え・生成識別列の検査を変更しました。内容が異なる同一キーは全行を除外します。3行が A・A・B の場合も3行とも除外し、キーと元の行番号を出します。処理経路の日本語診断、行内の複数セルの列挙、集計や複数列更新の途中結果を残さないことも検査します。既存の停止期待10項目を新しい行・件数・警告の期待へ変更し、保存済み台帳の識別・状態・契約と共有・終了ガードの検査は保持しています。

C#回帰104件、静的42件、Web20件、Win98ブラウザー28件、README完成例5件・派生例3件、入力境界7件、エラー案内8件、入力の緩和9件、区切り文字5件、ファイルログ11件、パッケージ13件を実行しました。今回のブラウザー検査は旧画像との画素一致を実行していません。リサイズの3条件では、ボタンに加えてメモ・備考の表示欄そのものが枠内の余白を保つことを確かめます。外枠だけの寸法では追従の証明になりません。

配布前の回帰で、共有ロックのCreateNewに負けた直後に他者が解放すると、File.Existsがfalseになって正常な競合を例外で返す不具合も再現しました。取得時のWindowsエラーコードで競合を判断し、取得後の書込失敗は握りつぶさず返します。修正後に既存の8スレッド検査を変更せず20回実行し、160件の状態変更が欠落せず保存されました（同一PC・ローカルファイル）。同じ回帰で落ちた警告の検査は、警告順の決め打ちとファイル名中の数字を拾う照合を、元ファイル名・物理行番号の組合せへ1回訂正しました。

実窓はEV2740S（EDID 26025105）・144 DPIの試験専用コピーです。候補5件から選んだ4件目の識別値00000812が、状態の送信後も表示され、未送信件数が1→0となりました。除外を含むレコード更新では、実窓が保存した990行・28列と全作業状態がRunUpdateの結果と一致しました（空キー10行、内容の異なる重複2行、不正値1行を除外）。拡大時も内側の表示欄が伸び、最小窓ではスクロール後に送信ボタンへ到達します。起動時の外寸は1249×996、指定画像1248×996に対して幅が1物理px異なります。設定は内容領域818×627です。監視の見本は無効で、起動時の入力警告は状態バーとログへ出ます。

証跡は`work/unexpected-20260909/`です（gitignore）。追加の使い捨て35条件の検査は2回修理した時点で退役しました。うち台帳4条件はシート名の違うfixtureを使っていたため、結果を採用しません。残る31条件の出力は有効です。台帳の複数列更新と数値並べ替えは、製品自身が生成した台帳を使う別の手動確認2件で確かめ直しました。実窓の値比較用コードの修理は1回（非公開クラス呼出しを公開XLSX読取へ変更）、表示欄の調査コードは1回（空のCSSを渡さない）、README例の期待値は1回（追加したskippedInvalidの0を明記）。試験窓・専用子プロセスは終了済みです。実IME、他DPI、現場CSV、本番共有、複数PC、性能再測定、新しいモデルによる委託検収は今回未実施です。

## 2026-09-09 直下の整理と実行ログの置き場

先生の指示で、リポジトリ直下の追跡ファイルを `.gitignore` / `LICENSE` / `README.md` / `ReaderDataViewer.cmd` / `ReaderDataViewer.vbs` / `THIRD-PARTY-NOTICES.md` / `build.bat` / `settings.json` の8本にしました。フェーズ7の監査報告は `docs/AUDIT_REPORT.md` へ、6テーマ版の配布一覧 `DELIVERY-MANIFEST.json` は `archive/multidesign-20260908/` へ移し、`output/` の出力CSV 3本は追跡から外して `/output/` を無視対象にしました（ディスク上のファイルは残しています）。実行ログ `paths.log` は出荷時の設定を `ReaderDataViewer.log`（アプリ一式のフォルダー直下）にし、READMEとK002/K005の説明を実際の既定値（もともとコードの既定は直下）に揃えました。`%LOCALAPPDATA%` のファイルログは変えていません。

## 2026-09-09 設定の「参照...」で固まる不具合と、旧式のフォルダー選択

先生が実機で見た「すぐ固まってボタンも押せなく、自動検索も効かなくなる」を試験用窓で再現しました。設定ダイアログのフォルダーの「参照...」は、旧式の`Shell.Application.BrowseForFolder`を所有者なし（hWnd=0）で、しかもWebView2のメッセージ処理の中で同期的に開いていました。開いている間はWebView2が後続のメッセージを配送しないため設定ダイアログのボタンが効かず、主画面はモーダル中で無効、自動検索もモーダル中は抑止されます。所有者が無いのでアプリの窓をクリックすると選択ダイアログが背後へ沈み、利用者には全体が固まったように見えます（9/7夜の起動ログには「すでに起動しています」の拒否が9回並んでおり、固まった窓を残したまま再起動を繰り返した形と一致します）。ファイルの「参照...」も主画面を所有者にしていたため、設定ダイアログの側は操作できる状態のままでした。

直し: `browse`と`window`のメッセージ、および例外時のエラー表示をキューへ回して（`PostOnUi`）ハンドラの外で実行する。両方の選択ダイアログの所有者を、開いている設定／出力のダイアログ面にする。フォルダー選択はOS標準の`IFileOpenDialog`（FOS_PICKFOLDERS、`src/Rdv3FolderDialog.cs`）にして、ファイル選択と同じ形式に揃えた（先生の「ファイルダイアログが新旧混在」に対応）。相対パスの初期位置はアプリ一式のフォルダー基準に解決する。

検査: `tests/Test-Browse.ps1`（実窓、WindowHandle・pid・出力JSONを渡す）は、フォルダー／ファイルの各選択ダイアログが設定面に所有されること、開いている間は設定面のHWNDが無効なこと、閉じたあと設定面が応答し主画面が有効へ戻ることを確かめて 3/3。撮影した2枚は`work/freeze-20260909/native/picker-0.png`、`picker-1.png`（gitignore）。C#回帰 104/104、static exit 0、theme-static 72/72（新ソース1本を指紋へ追加）。試験コードの修理は1回（所有されたダイアログはUIAで所有者の下に現れるため、列挙を主画面の子孫にも広げた）。先生の実機（win-note）と先生の操作手順そのものは未確認で、他の固まり方が残っていれば`%LOCALAPPDATA%/ReaderDataViewer/logs/feedback.log`の`UI NOT RESPONDING`と`PHASE`が次の証拠になります。

## 2026-09-09 コードレビュー（hyogo）で再現した6件の修正

C#回帰は104/104（既存98件と追加6件）。追加分は次を確かめます。日付を宣言したXLSXのキー列で、CSVからの高速結合が一般経路と同じ行を返すこと、変換後のキーに文字種・桁数の規則を適用し直すこと（漢字を含む書式はunicode、桁数が変わる書式はvariableの指定が要る）。1904年日付方式のブックが1900方式と同じ暦日になること。最後の通知が送信でも、読み直した内容が表示中と違えば戻った行を数えて切替を確認すること（未表示のWPF窓で実際のReloadSharedJobを呼び、確認を断った場合に旧内容が残る）。3状態以上の送信の記録が実際に保存した状態名で数えられ、他PCへの案内が推測した状態名を使わないこと。操作ログの控えを削除も切詰めもできない間に再送しても同じ行を二度追記せず、別プロセスからも再送しないこと。calculate等で作った列に宣言した型を、台帳へ渡す前の実行段階で検査すること。

hyogoが残した再現資料（`C:/repos-lab/reader-review-hyogo-20260909/probes`）を修正後の製品で走らせた結果は`work/review-fixes-20260909/probes/observations.json`。join-plainは高速経路でも結合1件、date-1904は`20260812`、derived-numberは識別001を示す型エラー、spool-delete-deniedは2回目のFlushでも2行のまま、three-statesは`Checked 0 件、Approved 1 件、Todo 0 件`、marker-sendは`changed=true reset=1`で切替確認へ進みました。

`python tests/test_static.py` exit 0、`test_theme_static.py` 71/71、`test_readme_example.py` 5/5、`test_readme_derived.py` 3/3、`test_relaxations.py` 9/9、`test_delimiter.py` 5/5、`test_input_boundaries.py` 7/7、`test_feedback_hints.py` 8/8。配布は`releases/build-20260909-111324-e3b62a46`（-RunTests）。試験コードの修理は1回（3状態の試験fixtureにtransitionsを足した。製品の判定は変えていない）。実窓・本番共有・複数PCは未実施です。

## 2026-09-09 端末別の操作ログ

C#回帰は98/98（既存94件と追加4件）。追加分は、操作ログのファイル名と保護（入力・ログ・出力に同名を使えない）、見出しを1回だけ書くこととCSVの引用、共有ファイルが開かれている間の控えと次回の順序どおりの再送、8スレッドが2端末名で同じフォルダーへ書いても端末ごとの1ファイルに行が欠けず混ざらないこと、更新ジョブが台帳を新規作成・更新したときだけ記録し、差分なし・書込み失敗では記録しないこと、通知（.version）が失敗しても記録が残ることを確かめます。

`python tests/test_static.py`、`test_theme_static.py`（71/71、新しいソース1本を指紋へ追加）、`test_readme_example.py` 5/5、`test_readme_derived.py` 3/3、`test_relaxations.py` 9/9、`test_delimiter.py` 5/5、`test_input_boundaries.py` 7/7、`test_feedback_hints.py` 8/8。配布は`build.bat package -Theme win98 -RunTests`で`releases/build-20260909-102524-6726c44d`を作成しました。

実窓（EV2740S、144 DPI、同梱データ）では、台帳の新規作成で`作成`、状態変更の送信で`送信`が同じフォルダーの`ReaderDataViewer-Ledger-操作ログ-<端末名>.csv`へ追記されました。操作ログを別プロセスが排他で開いたまま送信すると、台帳の置き換えと通知は完了し、その行は`%LOCALAPPDATA%/ReaderDataViewer/operations-*.spool.csv`へ控えられ、開放後の次の送信で控えの行→新しい行の順に追記されて控えが消えました。証跡は`work/oplog-20260909/native/`（gitignore）。複数PC・本番共有先・Excel実物での確認は未実施です。

## 2026-09-09 構造入力の除外と計算結果の説明

C#回帰は94/94（既存91件と追加3件）。列不足・空行の件数、元の行番号、未参照の重複列の除外と列位置、参照する重複の拒否、台帳生成・処理ダイアログの検証内容・headlessの一致を確認します。保存済み台帳や共有・終了ガードの検査は保持しています。

`python tests/test_input_boundaries.py` は7/7、`python tests/test_feedback_hints.py` は8/8、`python tests/test_readme_example.py` は5/5です。前者の重複見出しは台帳に保存する列なので引き続き拒否し、案内文の期待値を更新しました。4表例のsummaryは追加した3つの除外件数を0として照合します。既存検査の削除・skip・期待値の緩和はしていません。

実素材を使った単表のRunUpdateでは、列不足7行と空行3行を除外して29,993行。別表は未参照の重複2列を除外して18行、同じ重複名を保存する指定はexit 3でした。READMEの計算例はselectで未使用列を外してからcalculateし、保存結果6と250を確認しました。これらは業務全体の委託検収や、本番共有先・複数PCの確認を意味しません。

## 以前の検証記録

2026-09-09、共有更新と終了保留の組み替え後にWindowsで再実行しました。C#は30ファイルです。ブラウザー検査は模擬ブリッジを使います。これとは別に、実際のWPF/WebView2の窓をUI Automationから操作し、下記の範囲を確認しました。

| 種別 | この点検での実施状況 | 記録 |
|---|---|---|
| ブラウザーのJS／DOM | Chromiumで20項目通過 | `results/web-results.json` |
| 元ZIPの再現比較 | 過去の選定3項目の記録。今回は再実行していない | `results/original-web-reproductions.json` |
| 静的・サンプル整合 | 36項目通過 | `results/static-results.json` |
| Windows C#コンパイル | PowerShell 5.1で全ソースをコンパイル、exit 0 | 配布処理の`build-summary.json` |
| C#回帰試験63項目 | 63 / 63 PASS | `results/core-results.txt` |
| Win98の外観・ソース照合 | 53 / 53 PASS | `results/theme-static-results.json` |
| Win98のブラウザー回帰 | 29 / 29 PASS。旧Win98との主画面画素比較を含む | `results/theme-results.json` |
| WPF／WebView2／UI Automation | 144 DPI、同梱データで台帳作成・検索・候補選択・状態変更・両方向の送信・終了を確認 | `results/native-smoke.json`。IME、他のDPI、全ダイアログ等は未実施 |
| 実際の複数PC・SMB・障害回復 | **未実施** | `../docs/shared-ledger.md`の試験表 |

静的チェックが通ることはC#のコンパイル成功を意味しません。ブラウザーのブリッジを模擬した検査が通ることも、Windowsホストや共有台帳への保存が成功する証明ではありません。

## Windows上の検査

アプリフォルダーに移動したコマンドプロンプトで実行します。

```bat
build.bat compile
build.bat test
```

`compile`は起動時と同じソース統合・参照アセンブリーでコンパイルのみを行います。`test`は同じコンパイルに`RegressionTests.cs`を加え、テストメソッドを呼びます。通常のアプリ画面は起動しません。Windows PowerShellと.NET Framework／WPF、および同梱のWebView2管理DLLが読める環境が必要です。

63項目はJSON、正規表現、未送信の競合判定、再送、CSV、XLSX、出力保護、設定保存競合、ローカルセッション、共有ロック、8スレッドの競合書き込み等を含みます。元の50項目とJSON文字列の1項目に、共有更新・戻し通知・終了保留の12項目を追加しました。確認後の台帳変更、保存失敗、保存後の通知失敗、ロック待ち中の他者送信、終了とロック取得の競合、期限超過後の処理継続を再現します。**8スレッド試験は同一Windows環境の一時フォルダー上での試験**であり、複数PCやSMBの代替ではありません。

C#試験の設定と入力データは`tests/fixtures/`の隔離コピーを使います。ルートの運用設定や本番台帳を読み書きするテストではありません。試験用のCSV・XLSX・未送信データは新しい一時フォルダーに生成し、最後に削除を試みます。削除できなかった場合はその旨を表示します。試験結果はアプリ配下の`tests/results/core-results.txt`へ出力するため、その場所への書込権限は必要です。

終了コード0で全項目PASSです。失敗時には該当ケースと例外が出ます。コンパイルに失敗した場合はテスト自体が未開始であり、以前の結果ファイルを今回の合格結果と解釈しないでください。`native-status.json`はこの監査時点の固定記録で、自動更新しません。

## ブラウザー側の検査

Python 3とPlaywright、Chromiumを使います。例えば別の検証用Python環境で準備して実行します。

```text
python -m pip install playwright
python -m playwright install chromium
python tests/test_web.py
```

既存のChromium実行ファイルを指定する場合は次の形式です。

```text
python tests/test_web.py --chromium /usr/bin/chromium
```

HTML・CSS・JSの同梱ファイルをブラウザーへ注入し、架空のレコードとWebView2ブリッジを使って検査します。ファイルURLやHTTPサーバーのアクセス可否には依存しません。実アプリでのリソース読み込み・ナビゲーション制限・C#メッセージ処理の検証ではありません。

入力DOMの正規化、IME Enter、検索イベント数、無効化、重複ボタンID、モーダル、競合変更の保持／破棄、HTML文字列の扱い、出力の数式対策選択・拡張子等を確認します。IMEは`isComposing`等を設定した合成キーボードイベントを用いており、実際の日本語IME製品との相性検証ではありません。

出力ダイアログの状態を列選択・絞り込み・出力先へ分けた後、6項目を追加しました。左右の移動、キーボード選択と既定復帰、文字列条件と削除、数値・日付条件、検証エラーからの再試行、閉じた画面や別tokenへの遅い応答を確認します。追加検査の初回は19/20で、同じ検査を変更前の製品へ実行しても同じ1件が失敗しました。`f3`は表を包む要素のクラスであるため、追加検査のセレクタを1回訂正し、20/20となりました。既存14項目は削除・変更していません。

元ZIPを別に展開して比較する場合：

```text
python tests/test_web.py --baseline --root PATH_TO_ORIGINAL --output baseline.json
```

`--baseline`は元版の3件を再現するための選定モードです。元版に対しては3件のFAILと終了コード1が想定されます。これは修正版テストが失敗しているという意味ではありません。対象を区別せず集計しないでください。

## 静的・サンプル整合検査

Pythonの`pygments`、`json5`、PATH上のNode.jsを使います。

```text
python -m pip install pygments json5
python tests/test_static.py
python tests/test_static.py --original PATH_TO_ORIGINAL
```

C#の字句／括弧、文字列定数と追加ヘルパーの参照名、NodeのJS構文、同梱JSONC、入力CSVの形・キー・列参照を調べます。Pygmentsが誤認する有効なUnicodeエスケープ文字リテラルは字句検査時のみ置き換えます。C#の型検査、オーバーロード解決、IL生成、API互換性検査ではありません。

`--original`を指定すると元の全ファイルの残存と、入力・DLL・ライセンス類のバイト一致を確認します。現在はオプションなしで36項目、指定すると37項目になります。今回はオプションなしで実行しました。共有更新・戻し通知・終了保留の3ファイルを加えたため、ソースごとの字句検査が3項目増えています。ルートの設定・入力を書き換えた後はサンプル整合検査の条件や結果も変わります。

## 同梱の結果ファイルの時刻

結果の時刻はUTCです。日本時間の2026-09-08に行った検証が、JSON内では2026-09-07のUTC時刻になっています。各検証の実時刻・ブラウザー版・プラットフォームは結果ファイルを参照してください。
