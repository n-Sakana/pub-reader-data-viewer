# 実用 VBA 版アーカイブ

Reader Data Viewer の実用 VBA / Excel 版を退役時の内容で保全しています。

- `src/` — VBA モジュール
- `build/` — VBA 専用または C# / VBA 比較用だったビルド・検査・実測スクリプト
- `docs/` — FE/BE 分離、保存方式、設定、UI 仕様、実測の退役時点スナップショット

現行の配布物は C# 版だけです。この配下は通常ビルドへ接続せず、再開を決めたときに独立して復元します。退役直前の実行可能な配置は commit `017bb3e` です。
