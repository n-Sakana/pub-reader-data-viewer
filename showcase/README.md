# VBA Pixel Bridge — Excel分離アーキテクチャ 技術ショーケース

要件定義 v2 の実装。正本は同じフォルダの 2 文書：

- `VBA Pixel Bridge — Excel分離アーキテクチャ 技術ショーケース 要件定義 v2.md`
- `VBA Pixel Bridge — Excel分離アーキテクチャ 技術ショーケース 要件定義 v2.html`

画面は v3 モック（`PIXEL BRIDGE モック v3.html`）の実寸に合わせてある。
これは reader-data-viewer 本番版の改修ではない。`showcase/` の中で完結する。

## つくりかた（ダブルクリック）

`showcase\ビルド.bat` をダブルクリックする。管理者権限も UAC も要らない。
ExecutionPolicy はこの呼び出しの中だけ `-ExecutionPolicy Bypass` で外すので、
利用者が設定を変える必要もない。

コンソールに 1 / 2 / 4 の選択肢が出る。**疑似ピクセル GUI の「1 セルが受け持つ
設計ピクセル数」**を選ぶと、そのぶんだけをビルドする。

```
   1 ... 1px   仕様どおりの細かさ。盤面 812 x 812 = 659,344 セル
   2 ... 2px   中間。            盤面 406 x 406 = 164,836 セル
   4 ... 4px   いちばん軽い。    盤面 203 x 203 =  41,209 セル
```

- **既定値は置いていない。** 何も入れずに Enter を押しても、知らないうちに 4px が
  選ばれたりはしない。「何も入力されませんでした」と言って選び直させる。
- 選んだ値は成果物の名前（`dist\VBA Pixel Bridge 2px.xlsm`）と画面フッタ
  （`1 セル = 2px`）の両方に出る。開いたあとでも、どれを選んだか分かる。

引数で選ぶこともできる。ダブルクリックしたときは引数が無いので対話メニューへ進む。

```
ビルド.bat 2          … 2px で作る
ビルド.bat 3          … 選べないので何も作らず終わる（exit 1）
```

コマンドから直接叩くときも同じで、`-Unit` は必須：

```powershell
powershell -ExecutionPolicy Bypass -File showcase\build\build_showcase.ps1 -Unit 2
```

## 成果物

| もの | 何か |
|---|---|
| `dist/<部品名>_<N>px.bas` / `.cls` | 仕様上の成果物。VBA のソース一式（ANSI / CP932）。標準モジュール 4 枚とクラス 12 枚 |
| `dist/VBA Pixel Bridge <N>px.xlsm` | それらを取り込み、UIAutomationClient の参照を付けたブック |

`src/` は UTF-8 で持ち、ビルドが CP932 へ直して `dist/` に出す。往復で 1 文字でも
変わったら、そこで止める（`〜` のように CP932 を通ると別の字になるものがある）。
クラスモジュールの先頭にはクラスヘッダ（`VERSION 1.0 CLASS` …）があり、これは
CRLF でないと取り込み側がヘッダとして認識せず、そのままコード 1 行目に流れ込む。
取り込みは成功して見えるので、ビルドは書き出す前に必ず CRLF へそろえる。

参照設定は既定の 4 つ（VBA / Excel / stdole / Office）に **UIAutomationClient を
1 つ足すだけ**。Win32 API / `Declare` / `Shell` / `WScript.Shell` / 外部 helper は
1 つもない。図形（Shape）もフォームコントロールも ActiveX も使わない。画面は
すべてセルの塗りと結合セルで描いてある。

ブックにはこのソース一式のほかに、`ThisWorkbook` へ数行だけ入れてある。
ポンプの見張り（`PbEnsureArmed`）と、閉じる合図（`PbPrepareClose`）を呼ぶだけで、
判断はすべてソース側にある。標準モジュールからはブックのイベントを取れないため。

## 中身の構成（責務ごと）

状態と副作用はクラスに閉じ込め、標準モジュールは公開の入口と、状態を持たない
計算だけを持つ。Excel から名前で呼ばれるもの（`Auto_Open` / `OnTime` / `OnKey` /
`ThisWorkbook` のイベント）は VBA の都合で標準モジュールにしか置けないので、
`modPixelBridge` はその受け口に徹し、受けたらすぐ `PbApp` へ渡す。

| 部品 | 種類 | 受け持ち |
|---|---|---|
| `modPixelBridge` | .bas | 公開の入口だけ。判断を持たない |
| `modPbDesign` | .bas | 寸法・色・`PB_UNIT`・座標計算（状態なし） |
| `modPbCommon` | .bas | UIA の識別子と文字列ユーティリティ（状態なし） |
| `modPbBackend` | .bas | BE 側の入口と円周率の計算。コピーされたブックの中だけで動く |
| `PbApp` | .cls | 本体。1 秒ポンプ、操作の割り振り、起動と後始末 |
| `PbCanvas` | .cls | 疑似ピクセルの画布。シートの一生と描く道具 |
| `PbScreen` | .cls | 盤面の配置と差分描画、ボタンの台帳、ミニマップ |
| `PbUia` | .cls | UI Automation のクライアントと画面の矩形 |
| `PbNotepad` | .cls | つないでいるメモ帳 1 枚（読み書き・移動・最小化） |
| `PbWindows` | .cls | ミニマップに映す窓の一覧 |
| `PbChannel` | .cls | Temp のファイルだけで往復する FE ⇔ BE |
| `PbBeSession` | .cls | 非表示の別プロセス Excel の一生 |
| `PbDisplayBook` | .cls | 表示用 Excel（保存しない一時ブック） |
| `PbBench` | .cls | 15 秒の円周率ベンチ |
| `PbLog` | .cls | 記録。イミディエイト / ファイル |
| `PbError` | .cls | 例外の文脈（番号・説明・発生元・手続き・そのときの状態） |

### 例外と後始末

例外は起きた場所では判断しない。`PbError` が `Err` の中身とそのときの状態を
写し取り、外側の境界（`Startup` / `Tick` / 操作の入口 / `Shutdown`）で受けて、
記録と、必要なら明示的な後始末（別プロセスの停止・参照解放）へ回す。`Class_Terminate`
に後始末を任せない。VBA の解放順はこちらから見えず、別プロセスを持つ相手の
片づけを暗黙の順序に賭けると、消えないプロセスとして表に出る。

「起こりうる状態」は例外にしない。メモ帳が見つからない、`TransformPattern` が
取れない、BE がまだ答えていない、はすべて戻り値で返し、盤面の文言になる。

### 記録

`PbLog` は既定でイミディエイトウィンドウへ書く。ファイルへ切り替えるのは
`PbLog.cls` 冒頭の `LOG_TO_FILE` を `True` にする 1 行だけ。書き出しは行を溜めて
まとめて出す（1 行ごとに開いて閉じると、それ自体が 1 秒ポンプの中で目立つ重さに
なる）。区切りごとの所要時間（1 秒のティック、UIA の列挙・読み・書き戻し、
差分描画、JSON の読み書き、窓の操作、BE の起動と停止、COM の解放）は
`Span` で記録し、閾値（既定 30ms）より短いものは落とす。

## 使い方

1. **メモ帳を 1 枚開いておく。**つなぐのはこの 1 枚だけ。アプリはメモ帳を起動も
   終了もしない。開いている窓に UI Automation でつなぐだけ。2 枚以上開いていても、
   掴むのは 1 枚で、いったん掴んだらその窓を離さない（`FindAll` の並びは Z 順で、
   利用者のクリック 1 つで入れ替わる。並び順で選ぶと入れ替わった先へ前の窓の本文を
   書き戻して文章を消す）。
2. `dist\VBA Pixel Bridge <N>px.xlsm` を開く。「コンテンツの有効化」を押す。
3. `Auto_Open` が画面を組み、1 秒ごとのポンプが回りはじめる。このとき
   **Excel の窓を一度だけ画面の左半分へ合わせる**。以後、この窓は位置も
   大きさも変えない（owner 指示）。動かすのは、つないだメモ帳だけ。
4. メモ帳の動かし方は 2 つ。
   - **`最大化` / `最小化`**（ミニマップの右上）。`最大化` は OS の全画面最大化
     ではなく、**画面の右半分いっぱい**へ合わせる（FE は左半分のまま）。
     `最小化` はタスクバーへ格納する。掴んだ要素は手放さないので、そのあと
     `最大化` でも範囲選択でも戻せる。
   - **ミニマップの中をドラッグで矩形選択**すると、その矩形へ移動・リサイズする
     （`TransformPattern.Move` / `Resize`）。最小化されていても、これで戻せる。
   ミニマップを 1 セルだけクリックすると、その点にある窓の情報が
   `ElementFromPoint` の行に出る（動かす相手を選ぶ操作ではない。相手は常に
   つないだメモ帳 1 枚）。
5. 右下の `円周率計算` が本題。下を参照。
6. 閉じるときはふつうにブックを閉じる。表示用 Excel を閉じ、BE を終わらせ、
   Temp を消すところまで自動でやる。

### ショートカット（Application.OnKey）

`Ctrl+Shift+M` 同期の入 / 切 ・ `Ctrl+Shift+P` 円周率計算

アニメーションは常時 1 秒。速さの切替（5 秒 / なし）とそのボタンは廃止した。
1 秒で回らないなら作る意味がない、が owner の判断で、実際 1 秒で回るように
なったので切り替える理由も無い（下の「描画の直し」を参照）。

ボタンはセルで描いてあり、押下は「そのセルが選択されたこと」でポンプが拾う。
ホバーするとハイパーリンクの ScreenTip が説明を出す。

## 本題：BE が円周率を計算しているあいだ、FE は止まらない（15 秒）

`円周率計算` を押すと、こうなる。

1. **配置と準備**：表示用 Excel（保存しない一時ブック）を作り、
   FE を画面の左半分・表示用 Excel を右上 4 分の 1・メモ帳を右下 4 分の 1 に置く。
   三つが重ならずに画面を埋めるので、どれも隠れない。
2. **ここから 15 秒**を測る。非表示の別プロセス Excel（BE）が円周率を
   **実際に計算する**。既知の桁は 1 桁も使わない。
3. FE の 1 秒ポンプが BE の進捗を読み、まだ出していない桁を表示用 Excel の
   セルへ 10 桁ずつ流す。盤のランプは 1 つが 1 桁で、桁の値（0 が薄く 9 が濃い）
   がそのまま色になる。
4. **15 秒で自動終了**。表示用 Excel を保存せずに閉じ、BE を終わらせ、Temp の
   やりとりファイルをフォルダごと消す。結果の数字は盤面に残る。

**正式な結果は「表示用 Excel まで実際に届いた桁数」**。BE の中で何桁進んでいても、
画面に出ていない桁は数えない。だから FE には 3 つ出す ── 経過秒・計算できた桁・
表示できた桁。15 秒を越えたティックでは表示を足さない（1 秒ポンプなので、
最後に数えられるのは 14 秒台のティックまで）。

計算は Rabinowitz-Wagon の spigot（有限桁版）。桁を頭から 1 つずつ吐くので
「増えていく」様子がそのまま出せる。配列の長さ `ln = 10n/3 + 1` が「何桁まで
正しく出せるか」を決めるので、**途中で打ち切っても出した桁は正しい**。
1 桁あたり内側ループが `ln` 回なので総コストは約 `10n^2/3`。端末の速さは
決め打ちできない（開発機で決めた回数が非力なノートで 14 倍かかった、という
実測がこのリポジトリにある）ので、走る前に 0.15 秒だけ同じ内側ループを空回し
して速さを測り、15 秒で届く `n` を選ぶ。

本番ノート（1920×1080 / 125%）での実測。同じ端末でも `n` は測った速さで
決まるので、走るたびに多少ぶれる（下の 2px の 2 回がその例）：

| ビルド | 所要 | BE が計算した桁 | 表示用 Excel へ出せた桁（結果） |
|---|---|---|---|
| 4px | 15.70 秒 | 5,288 | **5,280** |
| 2px | 16.18 秒 | 5,147 | **5,140** |
| 1px | 15.19 秒 | 5,015 | **5,010** |

出た桁は毎回、独立実装（Gibbons の unbounded spigot を Python で書いたもの）と
**全桁一致**することを確かめてある。画面に出ている桁そのもの（表示用 Excel の
セル）も、既知の円周率の該当位置と突き合わせて一致を確認した。

登場人物は 4 つ。FE Excel（この疑似ピクセルアプリ）、BE Excel（別プロセス・
非表示・計算だけ）、表示用 Excel（BE とは別の一時ブック。書くのは FE）、
メモ帳（つないだ 1 枚）。やりとりは Temp の JSON（`command.json` /
`progress.json`）と、桁そのものを置く `pi.txt`。

**ベンチ中も、メモ帳の `最大化` / `最小化`、ミニマップの範囲リサイズ、双方向同期、
1 秒アニメはぜんぶ生きている。**それを見せるのがこのデモの主題なので、
実機の検収でも「回している最中に実際に押して動かす」ところまでやってある。

### 逃がすだけでは足りなかった（この実装でいちばん効いた修正）

最初の実装では、BE へ逃がしても **FE は同じように固まった**。原因は FE の仕事では
ない。FE の 1 ティックの中にある `ScanWindows`（UI Automation の列挙）が、計算中の
BE を待って返らなかった。実測：ティックの `t:sel` から `t:scan` までが 15.6 秒で、
BE の処理時間とぴたり一致した。

UIA の列挙は端から端まで全部のトップレベル窓に触りにいく。その中に「メッセージを
一切処理しないプロセス」が一つでもあると、そこで止まる。不可視でも窓は窓で、
そこに自分の BE がいた。**重い処理を別プロセスへ逃がすだけでは足りず、逃がした先が
応答し続けないと FE が巻き添えで固まる。**

直し方は BE のループに `DoEvents` を 1 つ入れるだけ。

ただし **毎回入れるのは高すぎる**。`DoEvents` は 1 回 1.6ms かかり（実測）、
円周率の 1 桁ぶんの計算より重い端末がある。毎桁だと 15 秒のうち数秒が
`DoEvents` に消える。いまは**時間で間隔を決めて 30ms に 1 回**にしてある。
応答は保てて、損は数 % で済む。

## ポンプは XLToolRack の JobPump に合わせてある

1 秒ごとの `Application.OnTime` の張り方・止め方・見張りは、`C:\repos\pub\xltoolrack`
の `src/addin/JobPump.bas` と同じ形にしてある。守るのは 1 つだけ：

- **予約は常に 1 本。** `m_armed` が真なら二重に張らない
- ティックは入口で `m_armed` を False に戻し（この予約は消費された）、本体の最後に
  もう 1 本だけ張る。**止めたいときは張らないだけ**でいい
- 見張り（`PbEnsureArmed`）はブック自身のイベントから呼ばれ、予約の時刻を 5 秒
  過ぎてもティックが来ていなければ張り直す。5 秒の猶予は、Excel がセル編集中や
  ダイアログ表示中に OnTime を後回しにするのを故障と取り違えないため

一度この不変条件を破って「ダイアログを閉じた直後にもう 1 本張る」を足したところ、
予約が同時に 2 本生きる状態ができ、閉じるときに取り消しきれなくなった。増築は
撤回して実績のある形へ戻してある。

## 要件 v2 §10「実装時に判断する事項」への答え（すべて実測）

| 判断事項 | 実測 | 採った答え |
|---|---|---|
| 0.5 秒ポーリングの実現方法 | `OnTime Now + 0.5s` は**即時発火**（8 tick が 0.016 秒）。`+1.5s` は 1.0 / 2.0 秒に丸まる | **1 秒**（§10 が認める「1 秒に戻す判断」）。XLToolRack の JobPump も 1 秒 |
| 重い処理の内容と、短すぎるときの調整 | 代入だけだと 10,000 セルで約 1.05 秒。短すぎて FE/BE の差が見えない | **BE が 15 秒だけ円周率を計算する**（2026-08-19 に差し替え。それ以前はメモ帳 2 枚の「原稿 → 清書」）。桁数は**実行時に端末の速さを 0.15 秒測って**選ぶ（開発機で決めた固定回数は、非力なノートで 212 秒かかった） |
| 疑似ピクセルのセル寸法 | 行高・列幅は表示 DPI の 1 デバイスピクセルに丸められる。開発機は 1 デバイスピクセル = 0.5pt、このノートは 0.6pt（125%） | 起動時に実測。ねらいは「1 デザイン px = 0.75pt」だが、**その寸法で盤面が画面に入らない端末では、入る最大の整数デバイス px に切り下げる**。1080p / 125% では 1px / 2px / 4px とも 1 セル = 1 / 2 / 4 デバイス px（盤面 812 デバイス px）になる |
| BE 用ブックの配置 | `AutomationSecurity = Low` はプログラムから開くときのマクロを通す設定で、置き場所の信頼設定とは別の口。`%TEMP%` のコピーでもマクロは動く（XLToolRack も同じやり方） | `%TEMP%\pixelbridge\` へ `SaveCopyAs` し、読み取り専用で開く。**配布物のフォルダには何も書かない** |
| UIAutomationClient の早期バインディング（tagPOINT） | `ElementFromPoint` は POINT を **ByVal** で取り、VBA は UDT を ByVal で渡せない。**コンパイル**エラーなので実行時には捕まえられない | 早期バインディング（参照設定）必須。点→要素は UIA の矩形ヒットテストで同義に実装 |
| DADS トークンの適用範囲 | — | 色・角丸・4px グリッド・Noto Sans JP を全面に適用。等幅は Noto Sans Mono、無ければ Consolas |

## 実機で測ってわかったこと（設計に効いた順）

1. **`ScreenUpdating` の False → True は、変えた量に関係なくビューポート全体を
   無効化する。その再描画の値段は盤面の複雑さ（色の切り替わり数）で決まる。**
   実盤面の 1px でトグル 1 回 2.4 秒（このノート）。逆に、True のまま少しだけ
   書けば汚した矩形しか描き直さず、同じ盤で直書き 1016ms / トグル 1711ms、
   素の 812×812（65 万セル）ならどちらも 1000ms（probe 実測）。**だから毎秒の
   ティックはトグルせず直書きし、Hold / Release は初期構築と後始末だけに
   する。この 1 点で 1px の 1 秒アニメが成立した**（旧実装は毎ティック
   トグルしていて、1px は 3.4 秒 / ティックまで落ちていた）。
2. **自分の窓の UIA 要素を作ると、以後そのティックのセル書き込みが 1 回
   130ms 級になる。** `ElementFromHandle(自分の hwnd)`（旧 実装は毎スキャン）と、
   フォーカスが Excel 上にあるときの `GetFocusedElement`（毎ティック）が該当。
   Excel 自身の UIA プロバイダがプロセスの中で動き出すため。probe では同じ盤・
   同じ UIA クライアントでも、自分の窓に触らない限り 1 write 5ms のまま。
   → 自分の矩形は `Application.Left/Top/Width/Height` × 実測 px/pt 比から作り、
   フォーカスは**メモ帳側の文書要素（外部プロセス）に `HasKeyboardFocus` を
   尋ねる**。自分の窓に UIA で触るのは起動時の pid 取得 1 回だけ（実測で無害）。
3. **背面の Excel が OnTime の中で出す `MsgBox` は、窓が作られない「見えない
   モーダル」になる。** OK が届くまでポンプごと止まり（実測 90 秒）、fe.json が
   止まるので BE が置き去り判定で自分から終わる、まで連鎖した。
   `vbMsgBoxSetForeground` でも `AppActivate` でも直らない。→ 完了ダイアログは
   その場で出さず、**利用者の次の操作（セル選択・ウィンドウ復帰）まで持ち越す**。
4. **`UIA_TransformPatternId` は 10016。10003 は RangeValuePattern。**
   ここを取り違えていたせいで、メモ帳の窓に `GetCurrentPattern(10003)` を
   投げて Nothing が返り、ログには `movewin: no transform` とだけ出ていた。
   例外は出ないので「このメモ帳は Transform を公開していない」という結論に
   見え、実際そう書いていた（2026-08-18 版の本文）。**それは誤りだった。**
   同じ端末・同じメモ帳 11.2606（Windows 26200）の窓要素は
   `GetSupportedPatterns` で 10009（Window）と 10016（Transform）を返し、
   VBA のネイティブ COM UIA からも `CanMove=True / CanResize=True` が取れ、
   `Move` / `Resize` が通る（2026-08-19 実測）。取り違えを助長したのは
   検証側の probe で、`IsTransformPatternAvailable` を 30025 で読んでいた
   （30025 は IsRequiredForForm。正しくは **30042**）。**プロパティ ID と
   パターン ID は、間違えても例外にならず「相手が公開していない」に化ける。**
   いまは失敗時のログに `avail=` を添えて、相手の問題か訊き方の問題かを
   その場で切り分けられるようにしてある。
5. **UIA のパターンを `As Object` で受けてはいけない。** パターンは IUnknown 系で
   IDispatch を持たず、`Set` の時点で失敗して Nothing 扱いになる（整列が「窓が
   動かない」ように見えた）。`IUIAutomationTransformPattern` 等の型で受ける。
   ID の取り違え（上）とこれは症状が同じなので、両方潰すまで原因が割れない。
6. **メモ帳の ValuePattern は改行を CR に正規化した文字列を返す**（CRLF を
   SetValue しても読み返すと CR）。控えを CRLF で持つと毎回「相手が変わった」と
   誤読する。控えは LF 正規形に統一する。
7. **セルの控えは「書いた値」ではなく「書いた結果セルが返す値」で持つ。**
   Excel は代入で先頭の `'` を落とす・数字や日付に化かすなど値を黙って直す。
   意図した値で控えると、次のティックが「セルが編集された」と誤読し、化けた
   文字列をメモ帳へ書き戻す（利用者の文字が勝手に変わる）。入力セルには
   `NumberFormat="@"` も併せて敷く。
8. **同期スロットは窓（hwnd）に貼り付ける。** `FindAll` の並びは Z 順で、
   利用者のクリック 1 つで入れ替わる。並び順でスロットを割ると、入れ替わった
   先へ前の窓の本文を書き戻して**利用者の文章を消す**。hwnd で対応を取り、
   結び直した直後の 1 回は必ずメモ帳 → セルの向きで揃える。
9. **表示設定の切り替えは、列幅を詰める前にやる。**
   グリッド線と見出しの切り替えは、そのシートの列と行を全部並べ直す。列幅が既定の
   うちは一瞬だが、812 列を 0.08 まで詰めたあと（1px）にやると **276 秒**かかった。
   シートを作った直後・幅を詰める前に同じ 3 行を実行すれば 0.1 秒。4px では気づけない。
10. **応答しないプロセスが 1 つあると、UIA の列挙はそこで止まる。**
   止めていたのが自分の BE だった（上の §）。忙しくする側にも `DoEvents` を入れる。
   ただし毎文字は過剰で、DoEvents 自体が仕事の 5 倍かかる（実測 1 回 1.6ms）。
   64 文字に 1 回で応答は保てる。
11. **`Input$(LOF(f), f)` で日本語のファイルは読めない。**
   これは「LOF バイト分の**文字**」を要求する読み方で、中身が ASCII のうちは動く
   （バイト数＝文字数）。CP932 の日本語は 1 文字 2 バイトなので必ずファイル末尾を
   越え、読み取り全体が落ちる。実測：原稿を渡した BE が毎回「原稿が読めません」を
   返した。`Open For Binary` + `Get` + `StrConv(b, vbUnicode)` で読む。
12. **窓の大きさは `UsableWidth` / `UsableHeight` に合わせてはいけない。**
   `UsableWidth` は縦スクロールバーを含み、`UsableHeight` はシート見出しと横
   スクロールバーを含む。合わせると設計の右端と下端が窓の外へ出る（実測：右下の
   ボタンが切れ、横スクロールバーが出た）。`ActiveWindow.VisibleRange` の列数・
   行数で、必要数より 1 つ多く見えるまで広げる。
13. **保存したブックに 1px グリッドのシートを残すと、開いた瞬間に固まる。**
   その場で作れば行高 812 行が 2 ms、列幅が 0 ms。毎回作り直し、終了時に消す。
14. **強制終了された FE は `fe.json` を残す。**
   BE が「ファイルが無い＝FE が消えた」だけを見ていると、残ったファイルを見て永遠に
   回り続ける（実測：FE を kill したら BE だけが残った）。中身が更新されているかで
   見る。ただし待つ長さは長めに（90 秒）。FE は正常でも黙る時間がある — 起動直後の
   最初の再描画がそれにあたる（1px なら数十秒）。
   逆に BE が自分から終わったら（`bye`）、FE は次のジョブで BE を起こし直す。
15. **ActiveX コントロールは現行 Office の既定で挿入できない**（1004）。テキスト入力は結合セル。
16. **小さいピッチでは文字の実描画が名目ポイントより広くなり、中央揃えの札が
   両端から欠ける**（実測：セル 3 デバイス px で「左右 2 分割」の左右が消えた）。
   ボタンの札は `ShrinkToFit` で幅に収まるまで縮めさせる。ただし
   **`ShrinkToFit` は結合セルには効かない**ので、頼りにするのは枠の実寸のほう。
17. **結合セルは 1 セル塗られただけで全体が塗り潰される。** 隣り合う要素が
   同じ行（列）を 1 つでも共有していると、片方の帯を塗った瞬間にもう片方の
   札がベタ塗りになって文字ごと消える。実測でこれが 2 か所あった：スロット 1 の
   札がスロット 0 の枠の下辺と同じ行に落ちていた（owner 報告の「片方が灰色に
   塗り潰される」）、進捗盤の下端と「待機」の札が同じ行だった。
   → **要素は 4 設計 px の格子に載せ、隣り合うものが同じセルを共有しない**。
18. **ミニマップの窓の矩形は画面の外へ出る。** メモ帳を画面の端に置く、Excel の
   窓がタスクバーの下へ沈む、といった普通の状態で矩形は画面境界を越える。
   そのまま描くとミニマップの枠を越えて隣のカードの上まで塗る（owner 報告の
   「青がはみ出す」）。**画面の矩形で必ず切り取る**。
19. **セルの表示形式を敷かないと、文字が値に化けて表示が変わる。** 稼働時間の
   `"01:07"` は時刻として取り込まれ、画面には `1:07` と出た（先頭の 0 が消える）。
   `NumberFormat = "@"` を先に敷く。ただし今度は `"0"` `"9"` のような札に
   「数値が文字列として保存されています」の**緑の三角**が出るので、そのセルだけ
   `Errors(xlNumberAsText).Ignore = True` で黙らせる（**`Range.Errors` は 1 セル
   にしか無い**。結合範囲のまま呼ぶと落ちるので `Cells(1,1)` へ）。
   利用者の Excel 全体の設定には触らない。
20. **文字の入る高さは PB_UNIT 倍のインセットで決めてはいけない。** 同じ設計でも
   4px は 40px・2px は 48px・1px は 52px になり、1 行 18 デバイス px の和文に
   対して 2px / 1px だけ 3 行目が半分描かれて切れた。**内寸は固定値**にする。
   同じ理由で、札の上下に PB_UNIT×2 ずつ空けると 4px では文字の高さが 8px しか
   残らず、和文の下半分が欠けた（フッタの `^+M 同期 入 / 切`）。
21. **盤面の外の余白には、1px ビルドだけ細い黒線が数本描かれる。** 行が 0.6pt まで
   潰れた盤で、結合セルのある行の帯が塗っていない余白側にはみ出して描かれる。
   セルの値・書式・結合・図形のどれを問い合わせても空のまま返る（実測）。
   変更前の版にも同じ位置に出ていた**既存の描画癖**で、**塗りのあるセルの上には
   出ない**。→ 盤を組んだあと、見えている余白を地の色で塗って隠す。

## 開発中の落とし穴（計測器が対象を壊していた）

`Application.OnTime` が BE の中で一度も発火しない、という現象を長く追った。結論は
**プラットフォームの制約ではなく、調べ方が壊していた**。

BE を覗くためのスクリプト（`verify/probe/inspect_be.ps1`）が、その Excel の VBE で
「VBAProject のコンパイル」を実行していた。自動化（`CreateObject`）で起こした Excel の
VBE を触ると、そのインスタンスは壊れる。以後 `OnTime` は `Err.Number = 0` のまま沈黙し、
常駐ループも 1 周で無応答になる。`Application.Run` は通るので「COM は生きているのに
OnTime だけ死ぬ」という紛らわしい見え方をする。しかも `FindControl(1,578)` は
`Enabled = False`（＝押せない）で返ることがあり、その場合 `Execute()` は何も検査して
いない。壊すだけで、確かめない。

VBE に触らない `verify/probe/probe_be_loop.ps1` で同じブックを走らせると、bootstrap は
8 ms で返り、常駐ループは `idle → running → done → bye` まで通ってプロセスが自分から
終わる。再現手順は `probe_be_loop.ps1 -Compile`。

## 仕様との差分（正直に）

| 仕様 | 実装 | 理由 |
|---|---|---|
| ポーリング 0.5 秒 | 1 秒 | 上表のとおり。§10 が想定している判断 |
| §6 の重い処理「100 行×100 列に代入」 | BE が 15 秒だけ円周率を計算し、その桁を FE が表示用 Excel へ流す | 代入だけでは 1 秒で終わり、FE/BE の差が体感できない。メモ帳 2 枚の「原稿 → 清書」も廃止（owner 指示 2026-08-19）。要件定義も更新済み |
| §6 完了時にダイアログ | 出さない。結果は盤面に残す | 完了ダイアログは利用者の**次のクリックを飲み込む**。実機で「ベンチ直後の `最大化` が効かない」として出た。経過・計算桁・表示桁は盤面に残るので、モーダルは要らない |
| 1 デザイン px = 0.75pt | 画面に入らない端末では、入る最大の整数デバイス px に切り下げ | 0.75pt どおりだと 1080p / 125% で盤面が 1015～1218 デバイス px になり、実行ボタンとフッタが最初から画面外だった（実測）。要件定義も更新済み |
| ミニマップは各ウィンドウを追従 | この Excel と、つないだメモ帳 1 枚 | デスクトップ全列挙は FE を止める。つなぐ相手は 1 枚に絞った（owner 指示 2026-08-19）。最小化した窓はミニマップには出さないが、掴んだ要素は手放さない（手放すと戻せない） |
| §7 整列（TransformPattern.Move / Resize） | **メモ帳 1 枚のみ移動・リサイズ。Excel は起動時に画面の左半分へ置いたきり動かさない**。ボタンは `最大化`（右半分いっぱい）と `最小化`（タスクバー） | owner 指示（2026-08-19）。`左右 2 分割` / `上下 2 分割` は廃止 |
| `GetFocusedElement` パネル | メモ帳の文書要素の `HasKeyboardFocus` で表示。メモ帳以外にフォーカスがあるときは「―」 | 自分の窓の UIA 要素を作ると全書き込みが 130ms 級になる（実測）。フォーカスが Excel にあるときの GetFocusedElement がそれに当たる |
| `ElementFromPoint` | UIA 矩形ヒットテストで同義実装 | VBA から呼べない（コンパイルエラー） |
| ElementFromPoint パネルの「アプリ名」 | 窓クラスからの推定 | プロセス名を引くには WMI が要り、それは許された外部機構の外 |
| Excel のシート枠・リボンを見せない | グリッド線・見出し・横スクロールバー・シートタブを消す。リボン・数式バー・ステータスバーはそのまま | owner 指示（リボン等は残す）。横スクロールバーとタブの 1 段（33 デバイス px）は 1080p で 812 行が入るかを分けるので消し、終了時に戻す |

## `PB_UNIT` を 1 / 2 / 4 で実測した結果（本番ノート 1920×1080 / 125%）

| `PB_UNIT` | 盤面 | 1 セルの実寸 | 起動（画面が出るまで） | 1 秒アニメ | 実画面確認 |
|---|---|---|---|---|---|
| 1px | 812×812 = 659,344 セル | 1 デバイス px | 約 3 秒 | **1.01～1.08 秒を保つ** | 済（本番ノート実機） |
| 2px | 406×406 = 164,836 セル | 2 デバイス px | 約 2 秒 | **1.00～1.07 秒を保つ** | 済（本番ノート実機） |
| 4px | 203×203 = 41,209 セル | 4 デバイス px | 約 2 秒 | **1.00 秒を保つ** | 済（本番ノート実機） |

この端末では 3 種とも盤面は同じ 812 デバイス px（1 デザイン px = 1 デバイス px）に
なり、違うのは絵の粗さだけ。どれも「選んだとおりの粒度で描画されること」を実画面で
確認してある。フッタの `1 セル = Npx` と `進捗盤 = ...セル` が選択値どおりになる。
**3 種とも 1 秒アニメのまま通常利用できる**（ティックの直書き化と、自分の窓に UIA で
触らない直しの結果。それ以前は 1px が 3.4 秒 / ティックだった）。
起動・アニメ・同期・書き戻し・終了までを、3 種それぞれ実機の実画面で通して
ある（2026-08-18。当時は「原稿 → 清書」デモ）。

2026-08-19 に、単一メモ帳と円周率ベンチへ作り替えたうえで 3 種とも実機で通し直した。
`showcase\ビルド.bat` を**ダブルクリックして実キーで 1 / 2 / 4 を選ぶ**正規経路で
作り、できた `.xlsm` を**ダブルクリックで開いて**操作している（引数指定も入力
リダイレクトも使っていない）。確かめたこと：

- 起動直後の Excel が画面の左半分（UIA / GetWindowRect で `-3,-2 960x1080`）に
  収まり、**その後の操作で 1 デバイス px も動かない**。`最大化`・`最小化`・
  ミニマップのドラッグ配置、いずれの前後でも矩形は同一（3 種すべてで確認）。
- `最大化` → メモ帳が `960,0 960x1080`（画面の右半分いっぱい）。`最小化` →
  タスクバーへ格納（UIA の矩形が消える）。そこから `最大化` でも、ミニマップの
  範囲選択でも戻る（4px `1230,600 660x432` / 2px・1px `1237,597 658x441`。
  いずれも UIA の実測矩形で、ドラッグした矩形と一致）。
- 単一メモ帳の双方向同期：セル → メモ帳、メモ帳 → セルとも 3 種で成立。
  掴んでいない方のメモ帳には 1 文字も書かない（別窓を開いたまま確認）。
- 円周率ベンチ（15 秒）は下の表。**ベンチの最中に `最大化` → `最小化` →
  ミニマップのドラッグを実際に押して、どれも効くこと**を 3 種で確認した
  （画面キャプチャと UIA 矩形の両方で）。同期も 1 秒アニメも止まらない。
- ティックの実測（生ログの `tick n: begin` の間隔）：

  | ビルド | 全体 | ベンチ中 |
  |---|---|---|
  | 4px | n=161 平均 1.041 秒 / 最大 2.100 秒 | n=18 平均 1.178 秒 / 最大 2.100 秒 |
  | 2px | n=129 平均 1.041 秒 / 最大 2.220 秒 | n=14 平均 1.234 秒 / 最大 2.150 秒 |
  | 1px | n=138 平均 1.068 秒 / 最大 2.770 秒 | n=14 平均 1.374 秒 / 最大 2.770 秒 |

  「1 秒設定」ではなく実動の値。最大側が伸びているのは、**検証側が外から UIA で
  XLMAIN を覗き、COM でセルを読み、毎秒スクリーンショットを撮っているため**
  （README の 2 の税が外部クライアントからでも掛かる。覗かずに測った素の値は
  1.02〜1.03 秒）。ベンチそのものは FE の負荷を上げない ── 上げているのは計測器。
- 円周率ベンチの結果（`ビルド.bat` 経由で作った成果物）：

  | ビルド | 所要 | BE が計算した桁 | 表示用 Excel へ出せた桁（結果） |
  |---|---|---|---|
  | 4px | 15.70 秒 | 5,288 | **5,280** |
  | 2px | 16.18 秒 | 5,147 | **5,140** |
  | 1px | 15.19 秒 | 5,015 | **5,010** |

  （途中の版では 2px が 2,509 / 2,500 になった回もある。走る前の 0.15 秒の計測で
  `n` が決まるので、端末が忙しいほど控えめになる。）

  桁は毎回、独立実装（Gibbons の unbounded spigot を Python で書いたもの）と
  全桁一致。走るたびに桁数がぶれるのは、BE が走る前に 0.15 秒だけ速さを測って
  `n` を決めるから（端末が忙しいほど控えめな `n` になる）。
  所要が 15.0 秒ちょうどではないのは、**終わりを見張っているのが 1 秒ポンプ**
  だから。15 秒を越えた最初のティックで畳むので、1 ティックぶん（実測で最大
  1.2 秒）遅れる。実測は 15.19〜16.18 秒。
  代わりに、**15 秒を越えたティックでは表示用 Excel へ桁を足さない**ので、
  「結果」として数える桁は必ず 15 秒以内に届いたものになる。
- 終了：ベンチ終了から約 10 秒で FE 以外の Excel プロセスは 0（BE は 1 秒ごとの
  ポーリングで quit を受け取ってから畳むので、消えるまで数秒かかる）。
  `%TEMP%\pixelbridge\` はフォルダごと消える。ブックを閉じたあとも Excel は 0。
- 開始前・計算中・終了直前・終了後の実画面をキャプチャして目視。灰色の塗り潰し、
  青のはみ出し、文字切れ、重なり、見えないダイアログのいずれも無し（3 種）。

開発機（1 デバイス px = 0.5pt の高解像度機）での旧実測は、旧実装（毎ティックの
ScreenUpdating トグル）に対するもので、1px「追いつかない」・2px「起動 17 秒」は
どちらも今の実装では再現しない。

## 検証

ビルドは書き出した全部品を不可視の Excel へ取り込み、保存する前に `PbPing` を
呼ぶ。VBA はモジュール単位に遅れてコンパイルするので、取り込みが通っただけでは
コンパイルが通ったことにならない。`PbPing` は入口から本体（`PbApp`）へ触るので、
どこか 1 つでもコンパイルが通らなければここで落ちる。返り値には取り込んだ部品数
（16）が入る。

```powershell
powershell -ExecutionPolicy Bypass -File showcase\verify\check_forbidden.ps1   # 禁止 API の機械検査（dist の全成果物）
powershell -ExecutionPolicy Bypass -File showcase\verify\e2e_notepads.ps1 -List
powershell -ExecutionPolicy Bypass -File showcase\verify\e2e_open.ps1 -Book "showcase\dist\VBA Pixel Bridge 4px.xlsm"
powershell -ExecutionPolicy Bypass -File showcase\verify\e2e_close.ps1         # ✕ 相当 (WM_CLOSE) で閉じて後始末を見る
```

`e2e_close.ps1` が閉じてよいのは **`e2e-owned.pid` 台帳にある、この検証が自分で
起こした Excel だけ**。走っている Excel を名前で拾って全部落とすことはしない。

`verify/probe/` は platform を測るために書いたもので、成果物ではない。

## 後始末（実測）

ふつうに閉じたあと：

- BE のプロセス … 終了（`quit` を送り、`bye` を待ってから消える。強制終了はしない）
- `%TEMP%\pixelbridge\`（BE 用コピー、command / progress / fe / pi.txt） … フォルダごと削除
- 配布物のフォルダ … 何も作らないので、消すものが無い
- `PIXELBRIDGE` シート … 削除（保存はしない）
- 利用者のメモ帳 … 無傷。アプリは起動も終了もしない
- 利用者の Excel … 触らない

## 未検証・積み残し

- **開発機で見えていた「FE プロセスが閉じたあとも残る」は、本番ノートの一連の
  検証（10 回以上の開閉）では一度も再現しなかった。** どの回も ✕ 相当の
  WM_CLOSE から数秒でプロセスまで消えている。開発機側の条件は切り分け未了のまま。
- ホバーツールチップ（ScreenTip）の見た目は自動では確かめられていない。
- 円周率の桁数は端末の忙しさで変わる（走る前の 0.15 秒の計測で n が決まる）。
  同じ端末でも 2,500〜5,400 桁の幅があった。速さの比較に使える数字ではない。
  ここで見せたいのは「BE が計算している間も FE が止まらない」ことのほう。
- ベンチの終わりは 1 秒ポンプが見張るので、所要は 15.2〜16.2 秒に散る
  （15 秒を越えた最初のティックで畳むため）。15 秒を越えたティックでは表示を
  足さないので、結果として数える桁は必ず 15 秒以内に届いたもの。
- 窓を `Application.Left = 0` で置くと、UIA / GetWindowRect は `-3` を返し、
  **見えている左端は画面の x=5 あたりに来る**（DWM の不可視枠のぶん）。
  デスクトップが 5 デバイス px だけ左に覗く。Windows が窓をスナップするときと
  同じ性質のずれで、実害は無いと判断して補正していない。
- 上の 21（1px の余白の線）は、隠しはしたが**原因の特定には至っていない**。
  セル側にはどの問い合わせでも痕跡が無い。
- メモ帳が 2 枚以上あるとき、掴むのは「最初に見えていた 1 枚」。どれを掴むかを
  利用者が選ぶ口は無い（owner 指示で対象は 1 枚に絞ってある）。
- 1 時間以上の連続稼働、ベンチの 3 回以上の連続実行は未確認。
- 不可視のバックグラウンド プロセスは OS に実行効率を絞られる（このノートで
  実測 4～5 倍）。Win32 なしでは優先度を変えられないので放置。「FE が生き続ける」
  という主題には影響しない。
