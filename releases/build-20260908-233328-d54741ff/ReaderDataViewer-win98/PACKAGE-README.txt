Reader Data Viewer - Windows 98 Classic

Start: ReaderDataViewer.vbs (or .cmd for console diagnostics).
Extract the entire ZIP first. Requires 64-bit Windows, Windows PowerShell 5.1, WPF and WebView2 Runtime.
Theme: win98 / motion: off / input data: sample
Native compile: passed / core tests: not_requested
This is a source-at-startup distribution, NOT a standalone EXE.
The original business settings are copied unchanged. Sample data is not your live data.
No live ledger, log, output or local pending changes were copied.
Review paths in settings.json BEFORE running a production copy.
Japanese instructions: docs/design-build.md (source build tools are in the developer ZIP).
